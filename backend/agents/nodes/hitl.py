# TODO Phase 3: Implement HITL interrupt node using LangGraph interrupt()
# This node pauses the graph, emits approved claims via WebSocket, and waits
# for the user to confirm/modify claims before resuming.
#
# In Phase 1 this node is NOT inserted into the graph. The edge structure
# in graph.py is left so it can be inserted without refactoring.
